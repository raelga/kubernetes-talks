apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: app-config
    envs:
      - app.env

secretGenerator:
  - name: app-secrets
    envs:
      - secrets.env

resources:
  - deployment.yaml
